name: Update WakaTime Stats

on:
  schedule:
    - cron: "0 0 * * *"  # Exécute le script une fois par jour (minuit UTC)
  workflow_dispatch: # Permet d'exécuter manuellement l'action

jobs:
  update-readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Fetch WakaTime Stats
        run: |
          curl -s -X GET "https://wakatime.com/api/v1/users/current/all_time_since_today?api_key=${{ secrets.WAKATIME_API_KEY }}" | jq -r '.data.text' > wakatime_stats.txt

      - name: Update README
        run: |
          sed -i "/<!--START_WAKATIME-->/, /<!--END_WAKATIME-->/c\<!--START_WAKATIME-->\n🕒 Temps passé sur ce projet : $(cat wakatime_stats.txt)\n<!--END_WAKATIME-->" README.md

      - name: Commit and push if changed
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git diff --quiet || (git add README.md && git commit -m "Mise à jour du temps passé sur le projet" && git push)
